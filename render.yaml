services:
  - type: web
    name: cablet
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: LOCAL_DATA_FILE
        value: /var/data/local-data.json
    disk:
      name: cablet-data
      mountPath: /var/data
      sizeGB: 1
